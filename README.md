# Online Movie Searcher
 
## API Key
The software makes use of the [OMDb API](https://www.omdbapi.com/) and therefore requires an API Key. One can be requested on the website. Once a key is obtained replace the example key in the [API_KEY_template file](Resources/raw/API_KEY) and rename the file to **API_KEY**.
